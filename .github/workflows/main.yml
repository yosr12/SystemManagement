name: Build and Push ECR Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and Push Docker image
        run: |
          docker build -t firstcontainerformyproject .
          echo ${{ secrets.AWS_SECRET_ACCESS_KEY }} | docker login --username ${{ secrets.AWS_ACCESS_KEY_ID }} --password-stdin 529239326430.dkr.ecr.eu-west-3.amazonaws.com
          docker push 529239326430.dkr.ecr.eu-west-3.amazonaws.com/firstcontainerformyproject:latest